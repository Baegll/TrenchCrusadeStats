DUCKDB_PATH=./trench.db
PORT=8080
TC_BASE_URL=https://synod.trench-companion.com/wp-json/synod/v1
TC_RATE_LIMIT=2                     # requests per second (1-2 TPS recommended)
ANALYTICS_API_KEY=dev-key
ANALYTICS_RATE_LIMIT=60             # requests per minute per key
ADMIN_USERNAME=admin
ADMIN_PASSWORD=dev-password

# Future / unused — placeholders for planned features
# TC_API_TOKEN=                     # if Synod API adds auth (P2)
# BACKUP_S3_BUCKET=                 # S3-compatible backup target (P3)
